describe('Configuración de Jest v3', () => {
  // Una prueba simple para confirmar que Jest encuentra y ejecuta archivos en la carpeta 'tests'.
  test('Verificar que 1 es igual a 1', () => {
    expect(1).toBe(1);
  });
});